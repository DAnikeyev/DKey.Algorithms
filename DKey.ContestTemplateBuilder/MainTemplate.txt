using DKey.Algorithms;
using DKey.CodeForces.Contest{ContestNumber};

namespace DKey.CodeForces;

public static class Program
{
    private static Solver _solver = new Solver{ContestNumber}{ProblemLetter}();
    public static void Main()
    {
        _solver.Run();
    }
}